{
  "name": "airi-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --target=bun --outfile=dist/server.js",
    "test": "curl -s http://localhost:3001/health | python3 -m json.tool"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.11",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "bun-types": "^1.2.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
